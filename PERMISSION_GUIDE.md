# 星光播放器 — 手机权限设置指引

> 安装 App 后，请按照您的手机品牌完成以下设置，确保后台播放不被系统中断。

---

## 所有手机通用（必须）

### 1. 允许通知权限
首次打开 App 时会弹窗请求通知权限，请点击 **"允许"**。

如果不小心拒绝了：
- 进入手机 **设置 → 通知 → 星光播放器 → 开启通知**

### 2. 锁定后台任务（推荐）
打开手机的 **最近任务** 界面 → 找到星光播放器卡片 → 点击 **锁定图标**（通常在卡片右上角），防止清理后台时误杀。

---

## 华为 / 荣耀（鸿蒙 / EMUI）

### 关闭启动管理
设置 → 电池 → 启动管理 → 找到 **星光播放器** → 关闭 **"自动管理"** → 手动开启以下三项：
- ✅ 允许自启动
- ✅ 允许关联启动
- ✅ 允许后台活动

### 关闭电池优化
设置 → 应用 → 星光播放器 → 电池 → 选择 **"不受限制"**

### 省电模式例外
如果开启了"超级省电"模式，需要将星光播放器加入白名单：
设置 → 电池 → 更多电池设置 → 休眠时始终保持网络连接 → 开启

---

## 小米 / 红米（MIUI / HyperOS）

### 省电策略
设置 → 应用设置 → 应用管理 → 星光播放器 → 省电策略 → 选择 **"无限制"**

### 允许自启动
设置 → 应用设置 → 应用管理 → 星光播放器 → 自启动 → **开启**

### 锁屏后保持运行
设置 → 更多设置 → 开发者选项 → 关闭 **"MIUI 优化"**（可选，部分机型需要）

---

## OPPO / realme（ColorOS）

### 电池优化
设置 → 电池 → 更多设置 → 优化电池使用 → 找到 **星光播放器** → 选择 **"不优化"**

### 允许后台运行
设置 → 应用管理 → 星光播放器 → 耗电保护 → 选择 **"允许后台运行"**

### 自启动管理
设置 → 权限隐私 → 自启动管理 → 星光播放器 → **开启**

---

## vivo / iQOO（OriginOS / Funtouch OS）

### 后台高耗电
设置 → 电池 → 后台高耗电 → 找到 **星光播放器** → **允许**

### 自启动
设置 → 权限与隐私 → 自启动 → 星光播放器 → **开启**

---

## 三星（One UI）

### 电池优化
设置 → 应用程序 → 星光播放器 → 电池 → 选择 **"不受限"**

### 关闭自适应电池限制
设置 → 电池和设备维护 → 电池 → 后台使用限制 → 确认星光播放器 **不在** "深度休眠应用"和"休眠应用"列表中

---

## 原生 Android（Pixel 等）

### 电池优化
设置 → 应用 → 星光播放器 → 电池 → 选择 **"不受限制"**

---

## 常见问题

**Q: 播放一段时间后自动停止了？**
A: 最可能的原因是电池优化未关闭。请按照上面您手机品牌的步骤，将星光播放器设为"不受限制"。

**Q: 锁屏后没有通知栏播放控制？**
A: 请检查通知权限是否已开启（设置 → 通知 → 星光播放器）。

**Q: 清理后台后播放停止了？**
A: 请在最近任务界面锁定星光播放器卡片，防止被一键清理。

**Q: 来电后播放没有自动恢复？**
A: App 会在通话结束后自动恢复播放。如果没有恢复，请点击通知栏的播放按钮。
